# Session Log: 2026-04-27_01

## 1. 개요 (Context)
- **일시:** 2026-04-27
- **주요 목적:** 로그인 401 에러 해결, 채팅/알림 응답 규격 최적화, 웹소켓 통신 경로 수정 및 채팅 정책 변경.

## 2. 주요 작업 및 결정 사항 (Decisions)

### [Security] 로그인 인증 정상화
- **이슈:** 비밀번호 "1234" 입력 시 401 Unauthorized 발생.
- **원인:** DB에 저장된 BCrypt 해시값이 잘못된 형식(접두사 누락 및 비정상 반복)이었음.
- **조치:** "1234"의 올바른 60자 해시값(`$2a$10$7EEEaZwVDEKVZo1yG0IV..PdJB3QAix6dXP68IooRpsaKjXrIK60q`)으로 DB 업데이트 가이드 제공.

### [API/Refactoring] 컨트롤러 안정성 및 규격 최적화
- **BoardController 방어 코드:** `keyword` 파라미터 누락 시 500 에러가 발생하던 문제를 `@RequestParam(required = false, defaultValue = "")`로 해결. `null.trim()` 방지 로직 적용.
- **응답 규격 수술적 리팩토링:** 채팅(`ChatController`) 및 알림(`NotificationController`) API에서 불필요한 `result` 계층을 제거하고 `data` 필드에 직접 매핑하도록 변경. 
  - **이유:** 프론트엔드의 `data.data` 접근 방식과 일관성을 맞추고 파싱 에러를 방지하기 위함.

### [WebSocket/STOMP] 실시간 통신 정상화
- **RedisSubscriber 경로 수정:** `/sub/chat/room` 뒤에 슬래시(`/`)가 누락되어 메시지가 전달되지 않던 치명적 오타 수정.
- **웹소켓 응답 통일:** 웹소켓으로 나가는 메시지도 `{success, code, msg, data}` 규격으로 래핑하여 프론트엔드 연동 편의성 증대.
- **캐시 방어:** `ChatController.getChatHistory`에 `no-store` 헤더를 추가하여 계정 전환 시 이전 유저의 채팅 잔상이 남는 보안 이슈 해결.

### [Policy] 오픈채팅 정책 변경
- **휘발성 채팅 도입:** 카카오톡 오픈채팅 스타일을 채택하여, 방 입장 시 과거 대화 내역을 불러오지 않도록 정책 변경 (백엔드 `getChatHistory` 빈 리스트 반환 설정).

## 3. 해결된 주요 이슈 (Troubleshooting)
- **401 Unauthorized:** 비밀번호 해시값 정상화로 해결.
- **500 Internal Server Error:** Board API 파라미터 기본값 설정으로 해결.
- **STOMP Subscribe Error:** 프론트 구독 주소 정정 및 백엔드 발송 경로 수정으로 해결.
- **React Key Duplicate:** 프론트엔드 메시지 수신 시 중복 체크 로직 가이드 제공.

## 4. 현재 프로젝트 상태 (Status)
- 채팅 및 알림 시스템의 백엔드-프론트엔드 통신 통로 완전 정상화.
- 전체 API의 응답 규격이 사용자 편의에 맞게 정교하게 다듬어짐.

## 5. 다음 작업 (Next Steps)
- **접속자 리스트(Presence):** 휘발성 채팅의 단점을 보완하기 위해 현재 접속 중인 유저 명단 표시 기능 추가.
- **Redis 캐싱:** 닉네임 조회 등 반복적인 DB 접근을 Redis 캐시로 최적화.
