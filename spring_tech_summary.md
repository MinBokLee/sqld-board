# 🍃 Spring Boot API Design Technical Summary

이 문서는 게시판 시스템 구축 과정에서 학습한 스프링 부트 API 설계 원칙과 RESTful 스타일의 핵심 기술 요소를 정리한 요약본입니다.

---

## 1. `@PathVariable` vs `@RequestParam`

API를 설계할 때 데이터를 전달받는 두 가지 주요 방식의 차이와 현업의 선택 기준입니다.

### 📍 `@PathVariable` (경로 변수)
- **방식**: `/api/boards/{boardCode}`
- **관점**: **"자원 식별(Identification)"**
- **특징**: 해당 데이터에 접근하기 위해 **반드시 필요한 고유한 열쇠**일 때 사용합니다. 이 값이 없으면 API 자체가 동작할 수 없음을 의미합니다. (404 Not Found)
- **현업 사례**: 
    - 특정 리소스의 상세 정보 조회 (`/users/105`, `/boards/B_FREE_01`)
    - 종속 관계가 명확한 하위 자원 조회 (`/boards/B_SQLD_01/categories`)

### 🔍 `@RequestParam` (쿼리 파라미터)
- **방식**: `/api/categories?boardCode=B_SQLD`
- **관점**: **"데이터 필터링(Filtering) 및 검색"**
- **특징**: 전체 목록 중에서 **특정 조건**으로 걸러내거나 부가적인 정보를 줄 때 사용합니다. 이 값이 없어도 API는 동작할 수 있으며(전체 반환 등), 결과값만 달라질 때 사용합니다.
- **현업 사례**: 
    - 페이징 및 정렬 (`?page=1&size=10&sort=desc`)
    - 검색어 필터 (`?keyword=안녕`)
    - 다중 조건 검색 (`?category=S01&useYn=Y`)

---

## 2. 리소스 네스팅 (Resource Nesting)

현업 전문가들이 `/api/board/{boardCode}/categories`와 같이 계층적인 URL을 고집하는 이유입니다.

### 📂 계층적 문맥 (Hierarchy & Context)
- **의미**: 주소 자체가 데이터의 **'위치'**와 **'소속'**을 나타내는 지도가 됩니다.
- **철학**: "A라는 데이터가 B라는 데이터 없이는 존재할 이유가 없을 때" 주소에 그 부모를 명시합니다.
- **장점**:
    1. **직관성**: URL만 봐도 `게시판 > 카테고리`라는 관계가 한눈에 들어옵니다.
    2. **일관성**: `/board/{id}/posts`, `/board/{id}/files`와 같이 규칙성 있는 API 확장이 가능합니다.
    3. **보안**: `/board/{id}/**` 패턴으로 특정 게시판에 대한 권한 설정을 일괄적으로 관리하기 용이합니다.

---

## 3. 실무 설계 팁 (Best Practice)

1. **명확성 > 길이**: URL이 조금 길어지더라도 의미가 불분명한 것보다 명확한 것이 훨씬 낫습니다.
2. **복수형 사용**: 리소스를 나타낼 때는 `/category`보다 `/categories`와 같이 복수형을 쓰는 것이 REST 표준에 더 가깝습니다.
3. **Fail-Fast**: 서비스 로직의 가장 앞단에서 유효성을 검증하여 잘못된 요청이 무거운 DB 작업을 시작하기 전에 즉시 차단하는 습관이 중요합니다.

---
*마지막 업데이트: 2026-04-16*
