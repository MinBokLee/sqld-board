<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
    <settings>
        <!-- <setting name="useColumnLabel" value="true"/> --><!-- 컬럼명 대신 컬럼 라벨을 사용 -->
        <!-- <setting name="cacheEnabled" value="false" /> --><!-- 설정에서 각 매퍼에 설정된 캐시를 전역적으로 사용할지 말지에 대한 여부 -->
        <!-- <setting name="callSettersOnNulls" value="true" /> --><!-- 가져온 값이 null일 때 setter나 맵의 put 메소드를 호출할지를 명시 Map.keySet() 이나 null값을 초기화할때 유용하다. int, boolean 등과 같은 원시타입은 null을 설정할 수 없다는 점은 알아두면 좋다.-->
        <setting name="jdbcTypeForNull" value="NULL" /><!--빈값들어올때 null 값으로 setter 한다 -->
        <setting name="returnInstanceForEmptyRow" value="true" /><!-- 기본적으로 모든 열들의 행이 NULL 이 반환되었을 때 null을 반환한다  -->
        <setting name="defaultStatementTimeout" value="15000" /><!-- 15초 데이터베이스로의 응답을 얼마나 오래 기다릴지를 판단하는 타임아웃을 설정  -->
        <setting name="mapUnderscoreToCamelCase" value="true"/><!-- 전통적인 데이터베이스 칼럼명 형태인 A_COLUMN을 CamelCase형태의 자바 프로퍼티명 형태인 aColumn으로 자동으로 매핑하도록 함-->
    </settings>

    <typeAliases>

        <typeAlias alias="hashmap" 						type="java.util.HashMap" />
        <typeAlias alias="map" 							type="java.util.Map" />
        <typeAlias alias="list" 						type="java.util.List" />
        <typeAlias alias="arraylist" 					type="java.util.ArrayList" />
        <typeAlias alias="camelHashMap" 				type="com.sqld_board.sqld.common.util.CamelHashMap" />

        <!-- sample -->
        <typeAlias alias="sampleDtoRes" 				type="com.sqld_board.sqld.dto.response.sample.SampleDtoRes"/>
        <typeAlias alias="sampleDtoReq" 				type="com.sqld_board.sqld.dto.request.sample.SampleDtoReq"/>


    </typeAliases>

</configuration>
