<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sqld_board.sqld.mapper.SampleMapper">

    <!-- SELECT -->
    <!-- SELECT -->
    <!-- SELECT -->

    <!-- 샘플 데이터를 조회하는 쿼리 -->
    <select id="searchMemberInfo" resultType="sampleDtoRes">
        SELECT SAMPLE_ID   /* SampleMapper.searchMemberInfo*/
             , SAMPLE_NAME
          FROM SAMPLE
    </select>

    <!-- INSERT -->
    <!-- INSERT -->
    <!-- INSERT -->

    <!-- 새로운 샘플 데이터를 등록하는 쿼리 -->
    <insert id="saveSample" parameterType="sampleDtoReq">
        INSERT INTO SAMPLE /* SampleMapper.saveSample*/
                    (SAMPLE_ID, SAMPLE_NAME)
        VALUES      (#{sampleId}, #{sampleName})
    </insert>

    <!-- UPDATE -->
    <!-- UPDATE -->
    <!-- UPDATE -->

    <!-- DELETE -->
    <!-- DELETE -->
    <!-- DELETE -->




</mapper>