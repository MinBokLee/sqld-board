# 📋 WebSocket 실시간 메시징 및 알림 시스템 구축 종합 보고서

본 문서는 프로젝트의 실시간 인프라 구축부터 댓글 알림 연동, 채팅 데이터 고도화 및 시스템 자동화까지의 모든 과정을 단계별로 상세히 기록한 마스터 가이드입니다.

---

## 1. 단계별 구현 히스토리 (The Chronicle)

### [Phase 1] 실시간 메시징 인프라 구축 (기초 공사)
가장 먼저 **인프라**를 구축한 이유는 실시간 소통을 위한 '고속도로'가 없으면 어떤 기능도 올릴 수 없기 때문입니다.

1.  **`WebSocketConfig.java`**: 메시지가 오고 갈 통로(Endpoint)와 주소 규격(Prefix) 정의.
2.  **`RedisConfig.java` & `RedisSubscriber.java`**: 서버 확장을 고려한 중앙 집중형 메시지 브로커 설정.
3.  **`StompHandler.java`**: 연결 시점에 JWT 토큰을 검증하는 실시간 보안 체계 구축.

### [Phase 2] 실시간 알림 서비스 연동 (Trigger 구현)
인프라 위에 실제 비즈니스 가치를 더하는 작업입니다.

1.  **`BoardServiceImpl.writeComment` 수정**: 댓글 등록 시점에 알림 신호 발생 로직 추가.
2.  **`NotificationStompService`**: 실시간 전파와 DB 저장을 통합 관리하여 데이터 무결성 확보.

### [Phase 3] 채팅 메시지 영속화 및 데이터 고도화
1.  **`ChatController/Service/Mapper` 분리**: 기술 계층과 비즈니스 계층의 명확한 분리.
2.  **`JOIN` 쿼리 최적화**: 대화 내역 조회 시 발신자 닉네임을 한 번에 가져오도록 구현.

### [Phase 4] 시스템 예외 대응 및 자동화 (Final Polish)
1.  **`WebSocketEventListener.java`**: 세션 종료 감지를 통한 자동 퇴장 메시지 발송 및 중복 발송 방지 로직(`alreadyLeft`) 구축.
2.  **`scheduler` 패키지 통합**: 시스템 운영에 필요한 모든 배치 작업(알림 삭제, 조회수 동기화 등) 일원화.

---

## 2. 기술 가이드: 성능 최적화 및 확장성 전략

### 💡 현재 구조: 동기식 즉시 저장 (Synchronous Write)
*   **특징:** 메시지 발생 시 즉시 DB 기록. 초기 서비스의 데이터 정합성에 최적화.
*   **수용 능력:** 동시 접속자 **약 1,000명 ~ 3,000명** 수준 감당 가능.

### 🚀 향후 고도화 로직 (Roadmap)
1.  **비동기 저장 (Asynchronous):** 메시지 배달과 DB 쓰기 스레드 분리.
2.  **쓰기 지연 및 배치 처리 (Write-Behind):** Redis 버퍼를 활용한 대량 메시지 일괄 기록.

---

## 3. 부록: 데이터베이스 설계 및 제약 조건 표준 (DB Engineering Standards)

### ✅ 문자셋(Charset) 및 정렬 규칙(Collation)
현대적인 웹 서비스를 위한 정석적인 데이터베이스 환경 설정 가이드입니다.

*   **Charset (`utf8mb4` vs `utf8mb3`)**: `utf8mb4`는 현대 업계 표준으로, 4바이트 이모지(😀, 🚀 등)를 완벽 지원합니다.
*   **Collation (`utf8mb4_general_ci`)**: 빠른 연산 속도와 영어 대소문자 미구분 검색을 통한 사용자 편의성을 제공합니다.
*   **설계 원칙**: 시스템 전체의 일관성을 위해 `ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;` 설정을 모든 테이블에 일관되게 적용합니다.

### ✅ 데이터 무결성 및 동시성 제어 (Data Integrity)
데이터가 꼬이지 않도록 보장하는 정석적인 DB 제약 조건 활용법입니다.

*   **시간 데이터 자동화**: `DEFAULT NULL` 대신 **`DEFAULT CURRENT_TIMESTAMP`**를 사용하여 DB 수준에서 생성/수정 시간을 정확하게 관리합니다.
*   **중복 방지 (UNIQUE KEY vs Java Logic)**:
    *   Java 코드 레벨의 체크는 찰나의 동시 요청(Race Condition) 발생 시 중복 저장을 막지 못할 수 있습니다.
    *   **`UNIQUE KEY`**는 DB 엔진 수준에서 중복을 원천 차단하는 **'최후의 보루'**이므로 필수적으로 설정합니다.
*   **제약 조건 명시**: `PRIMARY KEY`는 정의상 `NOT NULL`을 포함하지만, DDL의 가독성과 명확한 의도 전달을 위해 명시적으로 `NOT NULL PRIMARY KEY`를 작성하는 관례를 따릅니다.
*   **관계 및 연쇄 삭제 (`ON DELETE CASCADE`)**: 부모-자식 테이블 간의 관계를 DB 수준에서 선언하여 데이터 정합성을 확보하고 애플리케이션 로직을 단순화합니다.

### ✅ 기본키(PK) 설계 전략
1.  **단일 대리키 (`ID` PK + UNIQUE 조합)**: 이벤트/로그 성격의 테이블에 적합하며, 데이터 가공 및 프로그래밍적 간결함을 제공합니다.
2.  **복합 식별키 (Composite Key)**: 공통 코드 상세 등 부모에 종속적인 마스터 테이블에 적합하며, 구조적으로 데이터의 유일성과 관계를 동시에 표현합니다.

---

## 4. 핵심 요약 (Summary by Stage)

| 단계 | 핵심 목표 | 성과 | 비고 |
|:---:|:---|:---|:---|
| **기초** | 인프라 구축 | STOMP + Redis 보안 통로 개통 | 인프라 완공 |
| **연동** | 알림 시스템 | 댓글/대댓글 트리거 및 DB 저장 완성 | 비즈니스 가치 |
| **기록** | 채팅 영속화 | 히스토리 조회 API 및 닉네임 JOIN | UX 개선 |
| **안정** | 예외 대응 | 자동 퇴장 감지 및 중복 발송 방지 | 신뢰도 확보 |
| **운영** | 자동화 | 30일 알림 삭제 및 스케줄러 통합 | 운영 효율 |

---
**🏆 결론:** 
본 시스템은 정석적인 DB 설계 원칙과 고도화된 실시간 로직을 결합하여, 단순 기능을 넘어 상용 서비스 수준의 품질을 지향합니다. 🚀
